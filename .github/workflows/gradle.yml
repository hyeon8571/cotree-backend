name: Deploy Spring Boot + Redis + Monitoring to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up SSH key
        uses: webfactory/ssh-agent@v0.7.0
        with:
          ssh-private-key: ${{ secrets.EC2_SSH_KEY }}

      - name: Deploy via SSH
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} << EOF
            cd ~/cotree-backend || exit 1
            git pull origin main || exit 1

            # GitHub Secret에서 REDIS_PASSWORD를 가져와 EC2 환경변수로 export
            export REDIS_PASSWORD=${{ secrets.REDIS_PASSWORD }}

            # Docker Compose down -> build -> up (환경변수 주입)
            sudo docker compose down || true
            sudo docker compose build || exit 1
            sudo docker compose up -d || exit 1
          EOF
